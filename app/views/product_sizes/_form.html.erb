<%= form_with(model: product_size, local: true) do |form| %>
  <% if product_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_size.errors.count, "error") %> prohibited this product_size from being saved:</h2>

      <ul>
      <% product_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :collection_id %>
    <%= form.number_field :collection_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
